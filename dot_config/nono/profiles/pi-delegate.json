{
  "extends": "default",
  "meta": {
    "name": "pi-delegate",
    "description": "pi-mono coding agent delegation profile. Read-only cwd by default; callers layer --allow . per invocation to upgrade for write work."
  },
  "workdir": {
    "access": "read"
  },
  "filesystem": {
    "allow": [
      "$HOME/.pi/agent"
    ],
    "read": [
      "$HOME/.agents/skills",
      "$HOME/.local/share/mise/installs/pi",
      "$WORKDIR"
    ]
  }
}
