# Ignore OS-generated files
**/.DS_Store

# Ignore Zed lock files and temp artifacts
.config/zed/prompts/prompts-library-db.*/lock.mdb
.config/zed/prompts/prompts-library-db.*/data.mdb
.config/zed/.tmp*
.config/zed/private_empty_*

# Source files that exist only as symlink targets (don't deploy these to home root)
claude-settings.json
codex-config.toml
mise-config.toml
zed-settings.json

# Claude Code project context (lives in source dir only)
CLAUDE.md

# Firecrawl scrape dumps
.firecrawl/

# Script sources (versioned in repo, built to ~/.local/bin/ — don't deploy the source tree)
scripts/
